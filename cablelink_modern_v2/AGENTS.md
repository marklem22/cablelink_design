# CableLink Project Instructions

Before editing any HTML, CSS, JavaScript, or CodeIgniter file:

1. Read and follow `SKILL.md`.
2. Treat `assets/css/site.css` as the shared design-system source.
3. Reuse existing components before creating new ones.
4. Run the responsive checks defined in `SKILL.md`.